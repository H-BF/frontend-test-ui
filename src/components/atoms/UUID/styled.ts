import styled from 'styled-components'

const ResetedButton = styled.button`
  padding: 0;
  color: inherit;
  font: inherit;
  background: none;
  border: none;
  outline: inherit;
  cursor: pointer;
`

export const Styled = {
  ResetedButton,
}
